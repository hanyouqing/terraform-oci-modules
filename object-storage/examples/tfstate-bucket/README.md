# Terraform state bucket example

Creates a private, versioned Object Storage bucket suitable for the Terraform
`backend "oci"` used by this repository's Terragrunt layout.

Not for Cloudflare R2 (that belongs in product repositories).
