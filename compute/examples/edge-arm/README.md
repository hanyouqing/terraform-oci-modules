# Edge ARM compute example

Always Free–friendly `VM.Standard.A1.Flex` profile: **2 OCPU / 12 GB RAM / 50 GB boot**.

`user_data` is an optional base64 blob provided by the caller. The shared compute
module does not ship application cloud-init.

Pair with `vcn/examples/edge-public` for subnet and NSG IDs.
